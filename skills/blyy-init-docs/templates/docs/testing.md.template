---
last_updated: {{DATE}}
last_synced_commit: init
audience: ai-and-human
read_priority: 4
max_lines: 200
parent_doc: ARCHITECTURE.md
code_anchors:
  - <!-- Phase 2 自动填充：测试根目录、CI 配置文件 -->
applicable_project_types:
  - backend-service
  - microservice
  - fullstack
  - frontend-spa
  - cli-tool
  - library
  - data-pipeline
  - infrastructure
change_triggers:
  - 测试策略变更
  - 测试覆盖率要求变更
  - 新增/删除测试分层
  - CI 测试步骤变更
---

# 测试策略

<!-- AI-READ-HINT
PURPOSE: 项目的测试分层、运行命令、覆盖率要求与关键测试场景。
READ-WHEN: 用户问「如何运行测试 / 测试在哪写 / 覆盖率要求 / CI 失败」或写测试。
SKIP-WHEN: 写业务逻辑、修运行时 bug。
PAIRED-WITH: AGENTS.md（构建/测试命令）, modules/<m>/code-map.md（测试文件位置）
-->

> 返回 [架构文档](ARCHITECTURE.md)

## 测试分层

> **AI-RULE：仅列出代码中实际存在的层级。无对应目录的层删除整行。**

<!-- Phase 2 自动填充：从测试目录结构和测试框架推断 -->

| 层级 | 框架/工具 | 测试目录 | 运行命令 | 代码示例 |
|------|----------|---------|---------|---------|
| 单元测试 | <!-- Phase 2 --> | `<!-- dir -->` | `<!-- 命令 -->` | `<!-- file:line -->` |
| 集成测试 | <!-- Phase 2 --> | `<!-- dir -->` | `<!-- 命令 -->` | `<!-- file:line -->` |
| E2E 测试 | <!-- Phase 2 --> | `<!-- dir -->` | `<!-- 命令 -->` | `<!-- file:line -->` |

## 测试命令

```bash
# 运行全部测试
{{TEST_COMMAND}}

# Phase 2 自动填充：分层运行命令
```

## 覆盖率

<!-- Phase 2 自动填充：从 CI 配置或覆盖率工具配置提取 -->

| 项 | 值 | 来源 |
|----|----|------|
| 覆盖率工具 | <!-- Phase 2 --> | `<!-- file -->` |
| 覆盖率门槛 | <!-- Phase 2 / TODO[p2,metric-baseline,dev-team] --> | `<!-- file -->` |
| 报告位置 | <!-- Phase 2 --> | |

## 关键 E2E / 集成场景

> **AI-RULE：必须穷举所有 E2E / 集成测试场景。每条必须有「测试文件」列。**

<!-- Phase 2 自动填充：从 E2E 测试文件名 / describe 块提取 -->

| 场景 | 覆盖流程 | 测试文件 | 关联模块 |
|------|---------|---------|---------|
| <!-- Phase 2 --> | | `<!-- file:line -->` | |

## 测试规范

<!-- Phase 2 自动填充：从测试代码命名习惯推断 -->

- **测试文件命名**：<!-- Phase 2 -->
- **测试类/函数命名**：<!-- Phase 2 -->
- **Mock 策略**：<!-- TODO[p2,design-rationale,dev-team]: 团队对 mock vs 真实依赖的取舍 -->

---

## 高级章节（按需启用）

<!-- INCLUDE-IF: 项目接入了安全扫描工具（Snyk / CodeQL / SonarQube / Trivy / ...） -->

### 安全扫描

<!-- Phase 2 自动填充：从 CI 配置中识别安全扫描步骤。无则删除本节。 -->

| 扫描类型 | 工具 | 触发时机 | 配置位置 |
|---------|------|---------|---------|
| <!-- Phase 2 --> | | | `<!-- file -->` |

<!-- INCLUDE-IF: 项目维护性能基线 -->

### 性能基线

<!-- TODO[p3,metric-baseline,dev-team]: 若有性能基线测试，列出指标与基线值。无则删除本节。 -->
