# Repository Guidelines

<!-- Phase 2 自动填充：项目一句话描述 -->

## Project Structure

<!-- Phase 2 自动填充：从目录树生成 -->
```
{{PROJECT_STRUCTURE}}
```

## Build, Test, and Development Commands

```bash
# build
{{BUILD_COMMAND}}

# run
{{RUN_COMMAND}}

# test
{{TEST_COMMAND}}
```

## Code Conventions

<!-- Phase 2 自动填充：从项目文件推断语言和编码规范 -->
- {{LANGUAGE}} targets {{FRAMEWORK}}
- Indentation: {{INDENT_STYLE}}
- Naming: 遵循语言标准命名规范

## Task Entry Protocol（任务入口协议）

**任何任务开始前，必须先读 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)**，按其「AI 任务路由表」决定下一步读哪些文档，而不是直接搜索代码。

标准工作流：
1. 读 `docs/ARCHITECTURE.md` → 确认任务类型，查「AI 任务路由表」
2. 按路由表读对应的最小文档集（如 `modules/<m>/code-map.md`）
3. 通过文档中的 `file:line` 代码锚点直接定位代码位置
4. 只有在文档明确给出定位后，才去读/修改对应源码

**禁止**在未读 `docs/ARCHITECTURE.md` 的情况下直接用 Grep/Glob 搜索代码作为任务的第一步。

## Documentation Maintenance Rules

AI 工具在修改代码时必须遵循文档维护规则，详见 [docs/doc-maintenance.md](docs/doc-maintenance.md)。

## Security & Configuration

<!-- Phase 2 自动填充：从配置文件推断 -->
- Do not commit secrets
